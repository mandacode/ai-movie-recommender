# --- Core ML / recommenders ---
pandas>=2.2
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4      # TruncatedSVD (matrix factorization)
xgboost>=2.0           # learning-to-rank experiments

# --- Content features / semantic search ---
sentence-transformers>=3.0   # query embeddings (torch installed separately, CPU)

# --- API + database ---
fastapi>=0.111
uvicorn[standard]>=0.30
psycopg[binary]>=3.2   # Postgres driver (pgvector accessed via SQL casts, no extra adapter)
psycopg_pool>=3.2      # connection pool
python-dotenv>=1.0

# --- Dev / tests ---
pytest>=8.0
