# --- Core (baseline pipeline) ---
pandas>=2.2
numpy>=1.26

# --- UI / dashboard ---
streamlit>=1.36

# --- LLM chat layer ---
openai>=1.40
python-dotenv>=1.0

# --- Dev / tests ---
pytest>=8.0

# --- Later stages (uncomment as we add models) ---
# scikit-learn>=1.4        # collaborative filtering, utilities
# scipy>=1.12              # sparse matrices for CF
# xgboost>=2.0             # learning-to-rank
# sentence-transformers    # embeddings
# anthropic>=0.40          # LLM-based explanations
