# --- Core (pipeline + collaborative filtering) ---
pandas>=2.2
numpy>=1.26
scipy>=1.12          # sparse matrices for item-based CF

# --- UI / dashboard ---
streamlit>=1.36

# --- LLM chat layer ---
openai>=1.40
python-dotenv>=1.0

# --- Dev / tests ---
pytest>=8.0

# --- Later stages (uncomment as we add models) ---
# scikit-learn>=1.4        # utilities
# xgboost>=2.0             # learning-to-rank
